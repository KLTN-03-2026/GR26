# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# NeuralProphet — time-series forecasting
# Yêu cầu Python 3.11 (không tương thích Python 3.13)
neuralprophet==0.9.0

# Database — SQLAlchemy async + PostgreSQL driver
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
greenlet==3.4.0  # Required by SQLAlchemy async engine

# Database migrations (AI Service owns its own schema)
alembic==1.13.3

# Task scheduler
apscheduler==3.10.4

# Data processing
pandas==2.2.3

# HTTP client (gọi Open-Meteo, BE internal)
httpx==0.27.2

# Config từ .env
pydantic-settings==2.6.1

# JWT verification (dùng chung secret với Spring Boot HS256)
python-jose[cryptography]==3.3.0

# Logging
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
